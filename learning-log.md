# 学习记录

这个文件记录我从零开始学习机器学习、完成推荐系统小项目的过程。

---

## 2026-07-12｜线性模型

### 今天学了什么

- 机器学习通过已有数据学习参数，而不是手工随便设定参数。
- 在线性回归中，模型形式是 `f(x) = wᵀx + b`。
- `w` 表示各个特征的影响程度，`b` 是偏置。
- 最小二乘法通过最小化预测值与真实值的平方误差和，寻找合适的 `w` 和 `b`。
- 误差不能直接相加，因为正误差与负误差会相互抵消。
- 并且用numpy进行了代码实现
- 学习了成本函数，以及一维变量和二维变量的图像的变化，取最小值的几何图形展现

### 我目前的理解

线性回归是在所有可能的直线中，寻找一条总体上离训练数据最近的直线。
平方误差既避免了正负抵消，也会更重地惩罚较大的预测错误。

### 明天要做什么

- 继续学习《西瓜书》的对数几率回归。
- 看吴恩达课程中对应的 Logistic Regression 内容。
- 学习梯度下降

## 2026-07-13| 梯度下降和多特征

### 今天学了什么

- 成本函数在线性回归的体现
- 用梯度下降实现线性回归模型
- 使用numpy实现了梯度下降的线性回归
- 多特征的线性回归，采用向量化解决，在代码实现的时候使用numpy.dot可以简化运算
### 明天学什么
- 逻辑回归和对应的成本函数以及梯度下降实现

## 2026-07-14 | 逻辑回归和特征缩放

### 今天学了什么

- 为了防止有部分X与其他的差异太大，可以进行特征缩放，即归一化
- 在学习率的选择上面可以先以小的学习率开始尝试，逐步增大
- 多项式的回归，为了更好的贴合已有的测试数据
- 在线性回归的成本函数的基础上加上sigmoid函数即可形成逻辑回归的成本函数
- 取对数可以解决多个局部最小值的情况
- 梯度下降的代码实现

### 明天学学什么

- 正则化和对应的线性逻辑回归
